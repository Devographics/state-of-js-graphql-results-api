#import "./surveys.graphql"

"""
Completion ratio and count
"""
type Completion {
    total: Int
    count: Int
    percentage: Float
}

type Query {
    survey(survey: SurveyType!, year: Int!): Survey
}
